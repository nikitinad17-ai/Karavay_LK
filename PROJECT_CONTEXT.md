# Контекст проекта

## Цель

Личный кабинет покупателей АО «КАРАВАЙ» для оформления и сопровождения заказов. Один покупатель может управлять множеством получателей/торговых точек; целевой масштаб — около 600 точек.

## Версия 3.3

React становится основной реализацией. Vanilla 3.2 остаётся в истории Git, но больше не является активной кодовой базой.

### Готово

- React 18/Vite 5 и полный набор экранов 3.2;
- каталог из 111 демонстрационных товаров и 7 категорий;
- роли покупателя и получателя;
- создание, просмотр, повтор, корректировка и отмена заказа;
- окно проверки заказа и мобильная закреплённая панель;
- единые ограничения минимального количества и кратности лотка;
- блокировка повторной отправки;
- подтверждение сброса несохранённых правок;
- immutable-обновления заказов;
- request gate для зависящих от состояния загрузок;
- recovery UI при ошибке начальной загрузки;
- unit, jsdom integration и Playwright E2E;
- production base `/react-test/`.

## Архитектурные решения

- Состояние: Context + reducer, без внешнего state manager.
- Навигация: внутренний route state; URL-маршрутизация пока не нужна.
- Бизнес-правила количества: `src/orderRules.js`.
- Сетевой слой: `api()` и демонстрационный fetch-adapter в `src/mockApi.js`.
- Конкурентные загрузки: поколения запросов в `src/requestGate.js`; применяется к покупателю, матрице и последнему заказу.
- Сборка: Vite, статический base `/react-test/`.

## Следующие этапы

### Этап 2 — реальный контур доступа и КИС

1. Зафиксировать OpenAPI/JSON Schema и таблицу кодов ошибок.
2. Вынести mock API за явный dev-флаг.
3. Подключить реальную сессию, выход и восстановление доступа.
4. Подключить buyer/outlets, matrix, orders и documents поэтапно.
5. Добавить AbortController, таймауты, correlation ID и централизованное логирование.
6. Прогнать контрактные тесты и пилот на ограниченной группе покупателей.

### Этап 3 — эксплуатационная готовность

1. CI: `npm ci`, `npm run check`, Playwright и dependency audit.
2. Наблюдаемость: ошибки загрузки/отправки, latency, доля повторов и отказов.
3. Разделение окружений и секретов, CSP/security headers.
4. Виртуализация/серверный поиск для больших матриц и сотен точек.
5. Доступность и кроссбраузерная регрессия.

### Этап 4 — развитие продукта

- URL-маршруты и прямые ссылки на разделы/заказы;
- черновик заказа с восстановлением между сессиями;
- уведомления о статусах и документах;
- аналитика повторных заказов и подсказки по ассортименту.
