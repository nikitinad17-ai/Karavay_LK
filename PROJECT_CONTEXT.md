# КАРАВАЙ · Личный кабинет — контекст для продолжения работы

Актуально на 13 сентября 2026 года. Текущая версия интерфейса: **v3.2**.
Название проекта у пользователя: **«Личный кабинет»**.

Документ переносит смысл переписки, принятые решения, ограничения и состояние
файлов. Это рабочая передача контекста, а не дословная стенограмма.

## 1. Что делаем

Развиваем личный кабинет клиентов КАРАВАЯ. Сейчас это демо, которое постепенно
разбираем и улучшаем. По конспекту, полученному 13 сентября, PocketBase уже
развёрнут на собственной внутренней Ubuntu-VM. Интерфейс пока к нему не подключён.
Состояние VM сообщено пользователем; из этой среды сервер не проверялся.

Бизнес-иерархия: **Покупатель → Получатели / торговые точки**. Покупатель работает
со своими точками; получатель — со своей точкой. Пользователь уточнил масштаб:
**около 600 точек**, а не 5000. Число одновременных пользователей и реальная
нагрузка пока не известны.

В текущем интерфейсе есть Обзор, Оформление заказа, История заказов, Документы,
Профиль и Мои точки. Используются получатель, дата поставки, группа продукции,
каталог, количество в лотках и штуках, корзина, минимальная сумма и итог заказа.
Хлебобулочные и замороженные продукты оформляются разными заказами в текущем демо.

## 2. Как работать с пользователем

- Спокойно, поэтапно, без преждевременного усложнения. Его формулировка:
  «Не кипишуй, просто демо проект, понемног разберём по полочкам».
- Сначала изучать переданные файлы и реальные сценарии, затем предлагать
  изменения. Старые версии запрашивать, если без них нельзя разобраться.
- Различать обсуждение, принятое направление, реализованный код и проверенный
  результат. Не выдавать планы из старой переписки за уже работающие функции.
- Пользователь хочет разрабатывать IT-проекты вместе с ассистентом, сохранять
  исходники и вести понятный журнал изменений `readmegpt.md`.
- Он спрашивал о создании навыка (skill) в процессе сотрудничества. Отдельный
  навык для этого проекта пока не создавался.
- Ранее приоритетом был интерфейс. 13 сентября пользователь передал обновление
  о развёртывании PocketBase и попросил проанализировать его и добавить в проект.
  Подключение авторизации — описанный следующий этап; в этой задаче меняется документация.

## 3. Решения и границы достоверности

| Тема | Состояние |
| --- | --- |
| Среда разработки | Сейчас файловое демо. Рабочая эксплуатация ещё не подтверждена. |
| Сервер | По новому конспекту PocketBase 0.40.3 работает на внутренней VM `serv01WebLK` (`192.168.25.98`), через systemd. Исходящего интернета нет. Независимо не проверено. |
| КИС | Пользователь прямо сказал, что информации пока нет и преждевременные выводы делать не нужно. |
| Проверка прав | Для рабочей версии пользователь требует проверять права только на сервере, через свой веб-сервер. |
| Данные в HTML | В рабочей версии не должно быть встроенного общего набора клиентских демоданных и клиентской проверки паролей. |
| Защита VM и авторизация | По конспекту созданы `buyers`/`outlets` и применена миграция. В интерфейсе остаётся демо-вход. Полная защита VM, права и рабочая интеграция не проверены. |
| HTTPS | По конспекту временно оставлен HTTP в локальной сети; решение TLS отложено. |
| Интерфейс v3.2 | Изменения внесены в код и собраны в HTML; границы тестирования описаны ниже. |
| GitHub | Приватный репозиторий: https://github.com/nikitinad17-ai/Karavay_LK. Ветка `main` содержит снимок v3.2 и документы проекта. Исходная история сохранена в `docs-project/archive/source-history.bundle`. |
| Google Drive | Пользователь хотел хранить там файлы изменений. Доступ и синхронизация в рамках этой работы не настроены. |
| Проект «Личный кабинет» | Подготовлены файлы для переноса контекста. Прикрепление к одноимённому проекту ChatGPT не подтверждено. |

Уточнение про браузер: для отображения рабочего интерфейса ему всё равно нужны
разрешённые конкретному пользователю данные. Требование пользователя следует
воплощать как отсутствие встроенной общей базы клиентов, серверных секретов и
клиентского принятия решений о доступе. Формулировку «в браузер ничего передаваться
не будет» не следует превращать в технически невозможное обещание интерфейса
вообще без данных.

## 4. КИС: чего не предполагать

В старом обсуждении встречались предположения о сервисных учётных данных КИС,
методах `/payers`, `/clients`, наличии контактов, формате JSON и недостатках
спецификации. Пользователь позже явно остановил эти выводы.

В репозитории есть имена методов, адаптеры и демонстрационная реализация API.
Это описание текущего кода, **не подтверждение контракта реальной КИС**.
Старый README содержит исторические утверждения об интеграции, которые нельзя
использовать как свежие сведения о рабочей системе.

Не проектировать окончательную интеграцию по этим предположениям и не менять
её самовольно. Когда пользователь вернётся к КИС, получить фактическую документацию
или согласованный контракт и реальные обезличенные примеры ответов.

## 5. PocketBase, авторизация и VM — обновление на 13 сентября

Источник: [новый конспект](docs-project/Karavay-LK-auth-chat-full.md).
[Разбор и следующие шаги](docs-project/AUTH_REVIEW-2026-09-13.md) отделяют
сообщённое состояние сервера от проверенного состояния кода.

| Параметр | По сообщению пользователя |
| --- | --- |
| Хост / IP | `serv01WebLK` / `192.168.25.98` |
| ОС | Ubuntu Server 24.04.5 LTS, x86_64; точная версия не проверялась |
| Учётная запись ОС | `lindevadmin`, с sudo |
| Исходящий интернет | Отсутствует; бинарники и файлы переносятся через Windows/SCP |
| PocketBase | 0.40.3, `/opt/pocketbase/pocketbase` |
| Служба | `pocketbase.service`, enabled и active по конспекту |
| API / админка | `http://192.168.25.98:8090` / `http://192.168.25.98:8090/_/` |
| Коллекции | `buyers`, `outlets`; логин `kis_code` |
| Миграция | `1757304000_create_auth_collections.js` применена по конспекту; `@request.data.buyer` исправлено на `@request.body.buyer` |
| HTTPS | Временно HTTP, TLS отложен до уточнения сетевой инфраструктуры |

Этот статус заменяет прежнее «сервер только планируется». Проверка VM по сети
не выполнялась; доступные нам исходники не содержат самостоятельного файла
миграции, unit или `authApi.js`. Полный пример миграции и unit находится в конспекте.
Не запускать его повторно как новую установку поверх существующих коллекций.

Цепочка выдачи доступа: менеджер заводит покупателя; покупатель управляет доступом
своих точек; точка восстанавливает доступ через покупателя. Контакты, OTP,
первичная активация и точные ограничения действий ещё требуют проработки.
Параметры 2 vCPU / 2 GB RAM / 20–30 GB из старой переписки были ориентиром,
а не подтверждённой конфигурацией этой VM.

Перед подключением входа нужно проверить запрет изменения `buyer` и `kis_code`,
права на `active`, применение `must_change_password` и полномочия `manageRule`.
Наличие булевых полей и созданных коллекций не подтверждает исполнение этих правил.
Разбор относится к присланному тексту; фактический экспорт настроек не предоставлен.
Рабочая интеграция КИС, размещение HTML и UI сброса паролей пока не выполнены.
Технические и юридические заверения из конспектов отдельно не подтверждены.

## 6. Откуда взяты исходники и что в них находится

Пользователь передал:

- `Karavay-LK-repo-v3_1_src.zip` — исходный репозиторий.
- `Karavay-LK-v3_1_2026-09-09.html` — собранное демо.

Позднее были приложены копии с суффиксами `(1)` и `(2)`; при сравнении они
совпали с соответствующими исходными файлами. Рабочие изменения сделаны
поверх v3.1 с сохранением ранее существовавших правок и локальной Git-истории.

| Файл | Назначение |
| --- | --- |
| `app/app.js` | Состояние SPA, представления, обработчики событий, адаптеры текущего демо. |
| `app/api.js` | Демонстрационный API, а не развёрнутый сервер. |
| `app/products.js` | Данные каталога демо. |
| `app/styles.css` | Стили интерфейса и мобильная раскладка. |
| `app/index.template.html` | Шаблон сборки. |
| `app/index.html` | Вход для разработки с отдельными JS/CSS. |
| `build_inline.py` | Сборка интерфейса в один HTML. |
| `index.html` | Готовое демо в корне архива. |
| `tests/ui-regression.js` | Изолированные проверки логики интерфейса без браузера. |
| `tests/t2.js` | Браузерные сценарии Playwright. |
| `readmegpt.md` | Изменения v3.2, границы проверки и список ручных сценариев. |
| `PROJECT_CONTEXT.md` | Этот контекст для продолжения работы. |

Интерфейс написан на обычном JavaScript и CSS без фронтенд-фреймворка.
`build_inline.py` создаёт `app/standalone.html`; его содержимое копируется в
корневой `index.html`. Само наличие встроенного демо-API не означает, что
интеграция с PocketBase, рабочая авторизация или серверные проверки уже реализованы.

## 7. Что пользователь попросил последним и что сделано в v3.2

Пользователь выбрал явную кнопку **«Проверить заказ»**: перед отправкой показывать
только выбранные позиции, количества, суммы, получателя и дату. На телефоне
он хотел закреплённый итог и переход к проверке без прокрутки всего каталога.

Также он сообщил, что при нажатии на поля и свободные области ползунок уходит
вверх, и попросил проверить такие ситуации **по всему проекту**, включая меню,
случайные выходы и закрытие окон.

Найдены две основные причины:

- Обработчик искал ближайший `data-action`. У фона карточки был `close-modal`,
  поэтому обычный клик внутри карточки мог восприниматься как её закрытие.
- Общий `render()` заменял содержимое корневого узла и терял прокрутку
  вложенных контейнеров и фокус. Поиск дополнительно вызывал обычный `focus()`.

Реализовано:

1. Отдельное окно проверки заказа с выбранными товарами, ценами, количеством,
   суммами строк, общим итогом, получателем, адресом, датой и группой.
2. Отправка из проверки. Возврат к каталогу сохраняет корзину и фильтры.
3. Нижняя панель с итогом и кнопкой проверки при ширине до 1000 px.
4. Общий расчёт итогов для проверки, панели и сводки; учёт минимальной суммы.
5. Блокировка повторной отправки, пока запрос выполняется. При ошибке
   окно проверки и корзина остаются, сообщение выводится в окне.
6. Сохранение прокрутки страницы и вложенных контейнеров, фокуса и выделения
   при обновлениях того же раздела. Новый раздел начинается сверху.
7. Сохранение открытого мобильного меню при уведомлениях и перерисовке.
8. Клик внутри карточки больше не закрывает её как клик по фону.
9. Фон подтверждения и Escape не отменяют несохранённые правки. Для отката
   нужна явная кнопка «Отменить правки».
10. Суммы и кнопка корректировки обновляются сразу при вводе количества
    в карточке заказа; лотки и штуки учитываются отдельно.
11. Фокус ограничен верхним диалогом, фон недоступен для ввода; добавлена
    обработка Escape. Удалён дублирующий обработчик закрытия карточки.

`app/api.js` и `app/products.js` при этих правках не менялись. Готовая поставка:
`Karavay-LK-v3_2_2026-09-09.html` и `Karavay-LK-repo-v3_2_src.zip`.
Добавление этого документа обновляет документацию архива, а не версию интерфейса.

## 8. Что проверено, а что ещё нет

**Выполнено:** 44 изолированные проверки логики, синтаксическая проверка JS,
сборка HTML, проверка трёх встроенных скриптов, совпадение сборки с корневым
HTML, целостность ZIP и наличие файлов в нём.

Проверки охватывают формирование разметки шести разделов, состав и суммы
проверки заказа, меню, события внутри окон и на фоне, сохранение правок,
защиту от повторной отправки, обработку ошибки и успешной отправки.
Восстановление прокрутки и фокуса проверено **на модели DOM**, без движка раскладки.

**Не выполнено:** успешный прогон v3.2 в настоящем браузере, визуальная проверка
на телефоне и проверка экранной клавиатуры iOS/Android. Управляемый браузер
заблокировал открытие локального HTML; для существующих Playwright-тестов
локальный Chromium также был недоступен. Браузерные тесты обновлены, но их
нельзя называть пройденными.

Поэтому правильно говорить «исправлены обнаруженные причины и пройдены
проверки логики», а не «все проблемы прокрутки на всех устройствах устранены».
Развёртывание, нагрузка, рабочая авторизация и проверки доступа не тестировались.

## 9. Ближайшее продолжение

После обновления от 13 сентября подготовлен план подключения авторизации:
сверить серверную схему, уточнить ограничения прав, затем сделать `authApi.js`
и подключить интерфейс. Подробности — в [разборе](docs-project/AUTH_REVIEW-2026-09-13.md).
Само подключение не входит в текущую задачу переноса информации.

Проверки интерфейса v3.2 остаются актуальными:

Проверить:

- Длинный каталог: поля, плюс/минус, поиск, фильтры, пустые области, уведомления.
- Мобильную нижнюю панель и возврат из проверки на прежнее место каталога.
- Историю и карточки заказов: ввод, прокрутку, суммы, закрытие и подтверждение.
- Обзор, Документы, Профиль, Мои точки: меню, пустые области, раскрытие карточек,
  вертикальную и горизонтальную прокрутку.
- Двойное нажатие отправки, отказ и повторную попытку, успешную отправку.
- Телефон с экранной клавиатурой. Не путать ошибочный прыжок страницы с
  естественным смещением для показа поля над клавиатурой.

Полный ручной список — в `readmegpt.md`. Если проблема сохранится, нужны
конкретный экран, действие, браузер, устройство и по возможности короткая запись.

Команды из корня распакованного репозитория:

```sh
node tests/ui-regression.js
python3 build_inline.py
cp app/standalone.html index.html
```

В PowerShell вместо последней команды можно использовать:

```powershell
Copy-Item app/standalone.html index.html
```

Браузерный сценарий: `node tests/t2.js`, когда Playwright и Chromium доступны.
Для запуска локального демо достаточно открыть корневой `index.html`.

## 10. Как вести контекст дальше

Рабочий комплект для переноса: этот документ и актуальный архив исходников.
Сначала читать `PROJECT_CONTEXT.md`, затем `readmegpt.md` и соответствующий код.
Предыдущий HTML v3.1 не использовать как актуальную сборку.

После следующего изменения обновлять `readmegpt.md`: причина, результат,
проверки и ограничения. `PROJECT_CONTEXT.md` обновлять при смене решений,
версии, приоритета или статуса инфраструктуры. Секреты и рабочие пароли
в эти документы не записывать.

Исходники и документация находятся в приватном GitHub `nikitinad17-ai/Karavay_LK`.
Google Drive рассматривался ранее, но синхронизация с ним не настроена.


## 11. Сборка материалов и перенос на GitHub — 2026-09-10

Пользователь передал актуальный архив v3.2, HTML, контекст и конспект авторизации
и попросил собрать всё в проекте и завести репозиторий GitHub.
В подключённом аккаунте найден пустой приватный `nikitinad17-ai/Karavay_LK`;
он выбран для переноса: https://github.com/nikitinad17-ai/Karavay_LK.

Корневой README обновлён, старый сохранён в `docs-project/archive/`.
Конспект авторизации сохранён без изменений и помечен как исторический вариант.
Пример миграции из конспекта не перенесён в исполняемые файлы и не применялся.
Добавлен `AGENTS.md`, связывающий дальнейшую работу с актуальным контекстом.

Повторно пройдены 44 проверки логики, синтаксис трёх JS-файлов, сборка и её
побайтовое сравнение с присланным HTML. Код приложения не менялся.
Браузерные проверки и серверное развёртывание в этой задаче не выполнялись.
Исходные три коммита и тег v2.0 сохранены в Git bundle; GitHub начинает новую
цепочку коммитов с импортом актуального снимка, включая незакоммиченные в ZIP правки.

Упоминания отсутствия GitHub выше описывают прежние этапы.
На этапе 10 сентября Google Drive и сервер в этой работе не настраивались;
сообщение о развёрнутом PocketBase получено 13 сентября и отражено в разделе 5.
Публикация исходников на GitHub не является развёртыванием сайта. Материалы приложены в текущем проектном чате;
настройки и список файлов самого проекта ChatGPT отдельно не изменялись.


## 12. Анализ нового конспекта — 2026-09-13

Новый файл отличается от предыдущего: добавлены реальная внутренняя VM без
исходящего интернета, развёрнутый PocketBase, systemd, применённая миграция,
исправление `@request.body` и решение временно использовать HTTP.
Исходник сохранён без изменений в `docs-project/Karavay-LK-auth-chat-full.md`,
предыдущая редакция — в `docs-project/archive/Karavay-LK-auth-chat-full-2026-09-10.md`.

Добавлен `docs-project/AUTH_REVIEW-2026-09-13.md`: сравнение, сверка с кодом,
замечания к правилам, открытые решения и последовательность подключения.
Обновлены README, указатель документов, AGENTS.md и журнал изменений.
Приложение v3.2, HTML и сервер этой задачей не изменялись.
